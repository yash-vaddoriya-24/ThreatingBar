<h1>Select a Customer</h1>

<%= form_with url: assign_combo_path, method: :post, local: true do |f| %>
  <div>
    <%= label_tag :customer_id, "Select Customer" %>
    <%= select_tag :customer_id, options_from_collection_for_select(@customers, :id, lambda { |c| "#{c.name} (#{c.phone_no})" }), prompt: "Choose a Customer", required: true %>

  </div>

  <% @combo_ids.each do |combo_id| %>
    <%= hidden_field_tag "combo_ids[]", combo_id %>
  <% end %>

  <div>
    <%= submit_tag "Assign Combo", class: "btn btn-success" %>
  </div>
<% end %>
